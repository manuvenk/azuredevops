# Pipeline to run release pipeline test.

trigger:
  - main

pool: Default


stages:
  
  - stage: stage1
    displayName: Stage One
    jobs:
      
      - job: job1
        displayName: job one
        steps:
        
          - script: echo "Hello World"


          - task: PublishBuildArtifacts@1
            inputs:
              PathtoPublish: $(Build.ArtifactStagingDirectory)/index.html
              ArtifactName: test



